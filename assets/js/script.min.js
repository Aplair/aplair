function showToast(message, type) {
type = type || 'success';
const toast = document.getElementById('toastMsg');
const toastText = document.getElementById('toastText');
const toastIcon = document.getElementById('toastIcon');
const toastBar = document.getElementById('toastBar');
toastText.textContent = message;
toastIcon.textContent = type === 'success' ? '✓' : '✕';
toast.className = 'toast toast-' + type;
toastBar.style.animation = 'none';
void toastBar.offsetHeight; 
toastBar.style.animation = 'toastBar 3s linear forwards';
toast.classList.add('show');
clearTimeout(window._toastTimer);
window._toastTimer = setTimeout(function () {
toast.classList.remove('show');
}, 3200);
}
document.addEventListener("DOMContentLoaded", function () {
const successClose = document.getElementById('successClose');
if (successClose) {
successClose.addEventListener('click', function () {
document.getElementById('successOverlay').classList.remove('active');
});
}
var currentLang = window._initialLang || "en";
var currentBasePrice = 0;
function setLang(lang) {
currentLang = lang;
var isAr = lang === "ar";
localStorage.setItem('aplair_lang', lang);
document.documentElement.lang = lang;
document.documentElement.dir = isAr ? "rtl" : "ltr";
document.body.classList.toggle("ar", isAr);
var enBtn = document.getElementById("btn-en");
var arBtn = document.getElementById("btn-ar");
if (enBtn) enBtn.classList.toggle("active", !isAr);
if (arBtn) arBtn.classList.toggle("active", isAr);
document.querySelectorAll("[data-en]").forEach(function (el) {
el.textContent = isAr ? el.dataset.ar : el.dataset.en;
});
document.querySelectorAll("[data-en-html]").forEach(function (el) {
el.innerHTML = isAr ? el.dataset.arHtml : el.dataset.enHtml;
});
if (typeof renderCountryOptions === 'function' && typeof _countryRendered !== 'undefined' && _countryRendered) {
renderCountryOptions(countries);
}
if (typeof renderContactCountryOptions === 'function' && typeof _contactCountryRendered !== 'undefined' && _contactCountryRendered) {
renderContactCountryOptions(countries);
}
var activeBtn = document.querySelector(".btn-open-booking.last-clicked");
var modalPkg = document.getElementById("modalPackageName");
if (activeBtn && modalPkg) {
modalPkg.textContent = isAr ? activeBtn.dataset.packageAr : activeBtn.dataset.packageEn;
}
const contactPackageSelect = document.getElementById("contactPackage");
const contactPackageSelected = document.getElementById("contactPackageSelected");
if (contactPackageSelect && contactPackageSelect.value && contactPackageSelected) {
const selectedOption = document.querySelector(`#contactPackageOptions .custom-dropdown-option[data-value="${contactPackageSelect.value}"]`);
if (selectedOption) {
contactPackageSelected.textContent = isAr ? selectedOption.dataset.ar : selectedOption.dataset.en;
}
}
document.querySelectorAll("[data-lang-content]").forEach(function (el) {
el.style.display = el.dataset.langContent === lang ? "block" : "none";
});
}
window.setLang = setLang;
setLang(currentLang);
window.addEventListener('load', function() {
setLang(currentLang);
});
const enBtn = document.getElementById("btn-en");
const arBtn = document.getElementById("btn-ar");
if (enBtn) enBtn.addEventListener("click", function () { setLang("en"); });
if (arBtn) arBtn.addEventListener("click", function () { setLang("ar"); });
const hireBtn = document.getElementById("btn-hire");
if (hireBtn) hireBtn.addEventListener("click", function () {
const pricing = document.getElementById("pricing");
if (pricing) pricing.scrollIntoView({ behavior: "smooth" });
});
const modal = document.getElementById("bookingModal");
const openBtns = document.querySelectorAll(".btn-open-booking");
const closeBtn = document.getElementById("closeModal");
const addonChecks = document.querySelectorAll(".addon-check");
function updatePrice() {
let addonsTotal = 0;
let selectedAddons = [];
let selectedLabels = [];
if (document.getElementById("addonsSection").style.display !== "none") {
addonChecks.forEach(check => {
if (check.checked) {
addonsTotal += parseInt(check.dataset.price);
selectedAddons.push(check.dataset.id);
selectedLabels.push(check.dataset.labelEn);
}
});
} else {
selectedLabels = ["AI Voiceover", "AI Architectural Visualization"];
}
let total = currentBasePrice + addonsTotal;
const noPortfolio = document.getElementById("noPortfolio");
if (noPortfolio && noPortfolio.checked) {
total += Math.round(total * 0.4);
}
document.getElementById("summaryBasePrice").textContent = "$" + currentBasePrice;
document.getElementById("summaryAddonsPrice").textContent = "$" + addonsTotal;
document.getElementById("summaryTotalPrice").textContent = "$" + total;
return {
total: "$" + total,
addons: selectedLabels.length ? selectedLabels.join(", ") : "None"
};
}
openBtns.forEach(btn => {
btn.addEventListener("click", () => {
openBtns.forEach(b => b.classList.remove("last-clicked"));
btn.classList.add("last-clicked");
const pkgEn = btn.dataset.packageEn;
const pkgAr = btn.dataset.packageAr;
currentBasePrice = parseInt(btn.dataset.price);
document.getElementById("modalPackageName").textContent = currentLang === "ar" ? pkgAr : pkgEn;
if (pkgEn === "Premium real estate Bundle") {
document.getElementById("addonsSection").style.display = "none";
document.getElementById("premiumMessage").style.display = "block";
} else {
document.getElementById("addonsSection").style.display = "block";
document.getElementById("premiumMessage").style.display = "none";
}
addonChecks.forEach(c => c.checked = false);
if (noPortfolioCheck) noPortfolioCheck.checked = false;
updatePrice();
modal.classList.add("active");
document.body.style.overflow = "hidden";
});
});
if (closeBtn) {
closeBtn.addEventListener("click", () => {
modal.classList.remove("active");
document.body.style.overflow = "";
});
}
addonChecks.forEach(check => {
check.addEventListener("change", updatePrice);
});
const noPortfolioCheck = document.getElementById("noPortfolio");
if (noPortfolioCheck) {
noPortfolioCheck.addEventListener("change", updatePrice);
}
const submitBookingBtn = document.getElementById("submitBooking");
if (submitBookingBtn) {
submitBookingBtn.addEventListener("click", function () {
const { total, addons } = updatePrice();
const activeBtn = document.querySelector(".btn-open-booking.last-clicked");
const nameVal = document.getElementById("bookingName").value.trim();
const emailVal = document.getElementById("bookingEmail").value.trim();
const waVal = document.getElementById("bookingWA").value.trim();
const countryCode = document.getElementById("bookingCountryCode").value;
const briefVal = document.getElementById("bookingBrief").value.trim();
['bookingName', 'bookingEmail', 'bookingWA', 'bookingBrief', 'agreePolicies'].forEach(function (id) {
var el = document.getElementById(id);
if (el) { el.classList.remove('field-error'); el.parentElement.classList.remove('field-error'); }
});
var hasError = false;
if (!nameVal) {
document.getElementById('bookingName').classList.add('field-error');
hasError = true;
}
if (!emailVal) {
document.getElementById('bookingEmail').classList.add('field-error');
hasError = true;
}
if (!waVal) {
document.getElementById('bookingWA').classList.add('field-error');
hasError = true;
}
if (!briefVal) {
document.getElementById('bookingBrief').classList.add('field-error');
hasError = true;
}
if (hasError) {
showToast(currentLang === "ar" ? "⚠️ يرجى ملء جميع الحقول المطلوبة" : "⚠️ Please fill in all required fields", "error");
return;
}
const agreePolicies = document.getElementById("agreePolicies");
if (agreePolicies && !agreePolicies.checked) {
showToast(currentLang === "ar" ? "⚠️ يجب الموافقة على سياسة الاسترجاع وشروط الاستخدام" : "⚠️ You must agree to the Refund Policy and Terms of Service", "error");
agreePolicies.parentElement.classList.add('field-error');
agreePolicies.focus();
return;
}
if (waVal.length < 7) {
showToast(currentLang === "ar" ? "رقم الواتساب يجب أن يكون 7 أرقام على الأقل" : "WhatsApp number must be at least 7 digits", "error");
document.getElementById("bookingWA").focus();
return;
}
const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
if (!emailRegex.test(emailVal)) {
showToast(currentLang === "ar" ? "يرجى إدخال بريد إلكتروني صحيح" : "Please enter a valid email address", "error");
document.getElementById("bookingEmail").focus();
return;
}
const data = {
name: nameVal,
email: emailVal,
phone: countryCode + " " + waVal,
package: activeBtn.dataset.packageEn,
basePrice: "$" + currentBasePrice,
addons: addons,
totalPrice: total,
projectDetails: briefVal,
videoDetails: briefVal,
reference: document.getElementById('bookingReference').value.trim() || 'None',
portfolioUsage: (document.getElementById("noPortfolio") && document.getElementById("noPortfolio").checked) ? "HIDE (Extra Fee Apply)" : "Allowed",
policyAgreed: (document.getElementById("agreePolicies") && document.getElementById("agreePolicies").checked) ? "Agreed" : "No",
language: currentLang,
timestamp: new Date().toISOString()
};
const scriptURL = 'https://script.google.com/macros/s/AKfycbwBmYOubj6GYvc9i2MCfV9A9uPNNoRi6jds-IN_vKdRlLMytkCB8gF55I1uslOrvcQ6LQ/exec';
const submitBtn = this;
const btnText = submitBtn.querySelector('span');
btnText.textContent = currentLang === 'ar' ? 'جاري الإرسال...' : 'SENDING...';
submitBtn.style.pointerEvents = 'none';
submitBtn.style.opacity = '0.7';
console.log('Submitting data to Google Sheets:', JSON.stringify(data));
const params = new URLSearchParams(data).toString();
const iframe = document.createElement('iframe');
iframe.style.display = 'none';
iframe.src = scriptURL + '?' + params;
document.body.appendChild(iframe);
setTimeout(function () {
document.body.removeChild(iframe);
const successTotal = document.getElementById('successTotalValue');
if (successTotal) { successTotal.textContent = data.totalPrice; }
modal.classList.remove('active');
document.body.style.overflow = '';
document.getElementById('successOverlay').classList.add('active');
btnText.textContent = currentLang === 'ar' ? (btnText.dataset.ar || 'تأكيد الحجز →') : (btnText.dataset.en || 'SUBMIT BOOKING →');
submitBtn.style.pointerEvents = 'all';
submitBtn.style.opacity = '1';
}, 3000);
});
}
const contactPackageSelect = document.getElementById("contactPackage");
const contactPackageWrap = document.getElementById("contactPackageWrap");
const contactPackageSelected = document.getElementById("contactPackageSelected");
const contactAddonsContainer = document.getElementById("contactAddonsContainer");
const contactAddonsSection = document.getElementById("contactAddonsSection");
const contactPremiumMessage = document.getElementById("contactPremiumMessage");
const contactAddonChecks = document.querySelectorAll(".contact-addon-check");
const contactPackageOptions = document.querySelectorAll("#contactPackageOptions .custom-dropdown-option");
let contactCurrentBasePrice = 0;
if (contactPackageSelected && contactPackageWrap && contactPackageSelect && contactAddonsContainer && contactAddonsSection && contactPremiumMessage) {
contactPackageSelected.addEventListener("click", function (e) {
e.stopPropagation();
const isOpen = contactPackageWrap.classList.toggle("open");
const parentGroup = contactPackageWrap.closest(".form-group");
if (parentGroup) parentGroup.classList.toggle("pkg-group-open", isOpen);
});
document.addEventListener("click", function (e) {
if (contactPackageWrap && !contactPackageWrap.contains(e.target)) {
contactPackageWrap.classList.remove("open");
const parentGroup = contactPackageWrap.closest(".form-group");
if (parentGroup) parentGroup.classList.remove("pkg-group-open");
}
});
function updateContactPrice() {
let addonsTotal = 0;
let selectedAddons = [];
let selectedLabels = [];
if (contactAddonsSection.style.display !== "none") {
contactAddonChecks.forEach(check => {
if (check.checked) {
addonsTotal += parseInt(check.dataset.price);
selectedAddons.push(check.dataset.id);
selectedLabels.push(check.dataset.labelEn);
}
});
} else if (contactPackageSelect.value === "Premium real estate Bundle") {
selectedLabels = ["AI Voiceover", "AI Architectural Visualization"];
}
let total = contactCurrentBasePrice + addonsTotal;
const contactNoPortfolio = document.getElementById("contactNoPortfolio");
if (contactNoPortfolio && contactNoPortfolio.checked) {
total += Math.round(total * 0.4);
}
document.getElementById("contactSummaryBasePrice").textContent = "$" + contactCurrentBasePrice;
document.getElementById("contactSummaryAddonsPrice").textContent = "$" + addonsTotal;
document.getElementById("contactSummaryTotalPrice").textContent = "$" + total;
return {
total: "$" + total,
addons: selectedLabels.length ? selectedLabels.join(", ") : "None"
};
}
contactPackageOptions.forEach(function (opt) {
opt.addEventListener("click", function (e) {
e.stopPropagation();
contactPackageOptions.forEach(o => o.classList.remove("selected"));
this.classList.add("selected");
const val = this.dataset.value;
const price = parseInt(this.dataset.price) || 0;
contactPackageSelect.value = val;
contactCurrentBasePrice = price;
contactPackageSelected.textContent = currentLang === 'ar' ? this.dataset.ar : this.dataset.en;
contactPackageSelected.classList.add('has-value');
contactPackageWrap.classList.remove("open");
contactPackageWrap.parentElement.classList.add('filled');
contactPackageWrap.parentElement.classList.remove('field-error');
if (!val) {
contactAddonsContainer.style.display = "none";
} else {
contactAddonsContainer.style.display = "block";
if (val === "Premium real estate Bundle") {
contactAddonsSection.style.display = "none";
contactPremiumMessage.style.display = "block";
} else {
contactAddonsSection.style.display = "block";
contactPremiumMessage.style.display = "none";
}
}
contactAddonChecks.forEach(c => c.checked = false);
const contactNoPortfolio = document.getElementById("contactNoPortfolio");
if (contactNoPortfolio) contactNoPortfolio.checked = false;
const contactAgreePolicies = document.getElementById("contactAgreePolicies");
if (contactAgreePolicies) {
contactAgreePolicies.checked = false;
contactAgreePolicies.parentElement.classList.remove('field-error');
}
updateContactPrice();
});
});
contactAddonChecks.forEach(check => {
check.addEventListener("change", updateContactPrice);
});
const contactNoPortfolioCheck = document.getElementById("contactNoPortfolio");
if (contactNoPortfolioCheck) {
contactNoPortfolioCheck.addEventListener("change", updateContactPrice);
}
}
const submitContactBtn = document.getElementById("submitContact");
if (submitContactBtn) {
submitContactBtn.addEventListener("click", function () {
const nameVal = document.getElementById("contactName").value.trim();
const emailVal = document.getElementById("contactEmail").value.trim();
const waVal = document.getElementById("contactWA").value.trim();
const countryCode = document.getElementById("contactCountryCode").value;
const briefVal = document.getElementById("contactBrief").value.trim();
const refVal = document.getElementById('contactReference').value.trim() || 'None';
let pkgValue = contactPackageSelect.value;
let basePriceStr = '$' + contactCurrentBasePrice;
let addonsStr = 'None';
let totalPriceStr = '$' + contactCurrentBasePrice;
if (pkgValue) {
const priceInfo = updateContactPrice();
addonsStr = priceInfo.addons;
totalPriceStr = priceInfo.total;
}
['contactName', 'contactEmail', 'contactWA', 'contactBrief', 'contactPackageWrap', 'contactAgreePolicies'].forEach(function (id) {
var el = document.getElementById(id);
if (el) { el.classList.remove('field-error'); el.parentElement.classList.remove('field-error'); }
});
var hasError = false;
if (!pkgValue) {
document.getElementById('contactPackageWrap').parentElement.classList.add('field-error');
hasError = true;
}
const contactAgreePolicies = document.getElementById("contactAgreePolicies");
if (contactAgreePolicies && !contactAgreePolicies.checked) {
showToast(currentLang === "ar" ? "⚠️ يجب الموافقة على سياسة الاسترجاع وشروط الاستخدام" : "⚠️ You must agree to the Refund Policy and Terms of Service", "error");
contactAgreePolicies.parentElement.classList.add('field-error');
contactAgreePolicies.focus();
return;
}
if (!nameVal) {
document.getElementById('contactName').classList.add('field-error');
hasError = true;
}
if (!emailVal) {
document.getElementById('contactEmail').classList.add('field-error');
hasError = true;
}
if (!waVal) {
document.getElementById('contactWA').classList.add('field-error');
hasError = true;
}
if (!briefVal) {
document.getElementById('contactBrief').classList.add('field-error');
hasError = true;
}
if (hasError) {
showToast(currentLang === "ar" ? "⚠️ يرجى ملء جميع الحقول المطلوبة" : "⚠️ Please fill in all required fields", "error");
return;
}
if (waVal.length < 7) {
showToast(currentLang === "ar" ? "رقم الواتساب يجب أن يكون 7 أرقام على الأقل" : "WhatsApp number must be at least 7 digits", "error");
document.getElementById("contactWA").focus();
return;
}
const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
if (!emailRegex.test(emailVal)) {
showToast(currentLang === "ar" ? "يرجى إدخال بريد إلكتروني صحيح" : "Please enter a valid email address", "error");
document.getElementById("contactEmail").focus();
return;
}
const data = {
name: nameVal,
email: emailVal,
phone: countryCode + " " + waVal,
package: pkgValue,
basePrice: basePriceStr,
addons: addonsStr,
totalPrice: totalPriceStr,
projectDetails: briefVal,
videoDetails: briefVal,
reference: refVal,
portfolioUsage: (document.getElementById("contactNoPortfolio") && document.getElementById("contactNoPortfolio").checked) ? "HIDE (Extra Fee Apply)" : "Allowed",
policyAgreed: (document.getElementById("contactAgreePolicies") && document.getElementById("contactAgreePolicies").checked) ? "Agreed" : "No",
language: currentLang,
timestamp: new Date().toISOString()
};
const scriptURL = 'https://script.google.com/macros/s/AKfycbwBmYOubj6GYvc9i2MCfV9A9uPNNoRi6jds-IN_vKdRlLMytkCB8gF55I1uslOrvcQ6LQ/exec';
const submitBtn = this;
const btnText = submitBtn.querySelector('span');
btnText.textContent = currentLang === 'ar' ? 'جاري الإرسال...' : 'SENDING...';
submitBtn.style.pointerEvents = 'none';
submitBtn.style.opacity = '0.7';
console.log('Submitting data to Google Sheets:', JSON.stringify(data));
const params = new URLSearchParams(data).toString();
const iframe = document.createElement('iframe');
iframe.style.display = 'none';
iframe.src = scriptURL + '?' + params;
document.body.appendChild(iframe);
setTimeout(function () {
document.body.removeChild(iframe);
const successTotal = document.getElementById('successTotalValue');
if (successTotal) { successTotal.textContent = data.totalPrice; }
document.getElementById('successOverlay').classList.add('active');
btnText.textContent = currentLang === 'ar' ? (btnText.dataset.ar || 'تأكيد الحجز →') : (btnText.dataset.en || 'SUBMIT BOOKING →');
submitBtn.style.pointerEvents = 'all';
submitBtn.style.opacity = '1';
document.getElementById("contactName").value = '';
document.getElementById("contactEmail").value = '';
document.getElementById("contactWA").value = '';
document.getElementById("contactBrief").value = '';
document.getElementById("contactReference").value = '';
document.getElementById("contactPackage").value = '';
const pkSelected = document.getElementById("contactPackageSelected");
pkSelected.textContent = currentLang === 'ar' ? pkSelected.dataset.defaultAr : pkSelected.dataset.defaultEn;
pkSelected.classList.remove('has-value');
document.querySelectorAll("#contactPackageOptions .custom-dropdown-option").forEach(o => o.classList.remove("selected"));
document.getElementById("contactAddonsContainer").style.display = "none";
if (document.getElementById("contactNoPortfolio")) document.getElementById("contactNoPortfolio").checked = false;
if (document.getElementById("contactAgreePolicies")) {
document.getElementById("contactAgreePolicies").checked = false;
document.getElementById("contactAgreePolicies").parentElement.classList.remove('field-error');
}
['contactName', 'contactEmail', 'contactWA', 'contactBrief', 'contactReference', 'contactPackageWrap', 'contactAgreePolicies'].forEach(function (id) {
var el = document.getElementById(id);
if (el && el.parentElement) el.parentElement.classList.remove('filled');
if (el) el.classList.remove('field-error');
});
}, 3000);
});
}
});
const countries = [
{ iso: 'EG', flag: '🇪🇬', name: 'Egypt', ar: 'مصر', code: '+20' },
{ iso: 'SA', flag: '🇸🇦', name: 'Saudi Arabia', ar: 'السعودية', code: '+966' },
{ iso: 'AE', flag: '🇦🇪', name: 'UAE', ar: 'الإمارات', code: '+971' },
{ iso: 'KW', flag: '🇰🇼', name: 'Kuwait', ar: 'الكويت', code: '+965' },
{ iso: 'QA', flag: '🇶🇦', name: 'Qatar', ar: 'قطر', code: '+974' },
{ iso: 'BH', flag: '🇧🇭', name: 'Bahrain', ar: 'البحرين', code: '+973' },
{ iso: 'OM', flag: '🇴🇲', name: 'Oman', ar: 'عُمان', code: '+968' },
{ iso: 'JO', flag: '🇯🇴', name: 'Jordan', ar: 'الأردن', code: '+962' },
{ iso: 'LB', flag: '🇱🇧', name: 'Lebanon', ar: 'لبنان', code: '+961' },
{ iso: 'IQ', flag: '🇮🇶', name: 'Iraq', ar: 'العراق', code: '+964' },
{ iso: 'YE', flag: '🇾🇪', name: 'Yemen', ar: 'اليمن', code: '+967' },
{ iso: 'SY', flag: '🇸🇾', name: 'Syria', ar: 'سوريا', code: '+963' },
{ iso: 'LY', flag: '🇱🇾', name: 'Libya', ar: 'ليبيا', code: '+218' },
{ iso: 'TN', flag: '🇹🇳', name: 'Tunisia', ar: 'تونس', code: '+216' },
{ iso: 'DZ', flag: '🇩🇿', name: 'Algeria', ar: 'الجزائر', code: '+213' },
{ iso: 'MA', flag: '🇲🇦', name: 'Morocco', ar: 'المغرب', code: '+212' },
{ iso: 'SD', flag: '🇸🇩', name: 'Sudan', ar: 'السودان', code: '+249' },
{ iso: 'SO', flag: '🇸🇴', name: 'Somalia', ar: 'الصومال', code: '+252' },
{ iso: 'DJ', flag: '🇩🇯', name: 'Djibouti', ar: 'جيبوتي', code: '+253' },
{ iso: 'KM', flag: '🇰🇲', name: 'Comoros', ar: 'جزر القمر', code: '+269' },
{ iso: 'MR', flag: '🇲🇷', name: 'Mauritania', ar: 'موريتانيا', code: '+222' },
{ iso: 'US', flag: '🇺🇸', name: 'United States', ar: 'الولايات المتحدة', code: '+1' },
{ iso: 'GB', flag: '🇬🇧', name: 'United Kingdom', ar: 'المملكة المتحدة', code: '+44' },
{ iso: 'DE', flag: '🇩🇪', name: 'Germany', ar: 'ألمانيا', code: '+49' },
{ iso: 'FR', flag: '🇫🇷', name: 'France', ar: 'فرنسا', code: '+33' },
{ iso: 'IT', flag: '🇮🇹', name: 'Italy', ar: 'إيطاليا', code: '+39' },
{ iso: 'ES', flag: '🇪🇸', name: 'Spain', ar: 'إسبانيا', code: '+34' },
{ iso: 'PT', flag: '🇵🇹', name: 'Portugal', ar: 'البرتغال', code: '+351' },
{ iso: 'NL', flag: '🇳🇱', name: 'Netherlands', ar: 'هولندا', code: '+31' },
{ iso: 'BE', flag: '🇧🇪', name: 'Belgium', ar: 'بلجيكا', code: '+32' },
{ iso: 'CH', flag: '🇨🇭', name: 'Switzerland', ar: 'سويسرا', code: '+41' },
{ iso: 'AT', flag: '🇦🇹', name: 'Austria', ar: 'النمسا', code: '+43' },
{ iso: 'SE', flag: '🇸🇪', name: 'Sweden', ar: 'السويد', code: '+46' },
{ iso: 'NO', flag: '🇳🇴', name: 'Norway', ar: 'النرويج', code: '+47' },
{ iso: 'DK', flag: '🇩🇰', name: 'Denmark', ar: 'الدنمارك', code: '+45' },
{ iso: 'FI', flag: '🇫🇮', name: 'Finland', ar: 'فنلندا', code: '+358' },
{ iso: 'PL', flag: '🇵🇱', name: 'Poland', ar: 'بولندا', code: '+48' },
{ iso: 'CZ', flag: '🇨🇿', name: 'Czech Republic', ar: 'التشيك', code: '+420' },
{ iso: 'RU', flag: '🇷🇺', name: 'Russia', ar: 'روسيا', code: '+7' },
{ iso: 'TR', flag: '🇹🇷', name: 'Turkey', ar: 'تركيا', code: '+90' },
{ iso: 'GR', flag: '🇬🇷', name: 'Greece', ar: 'اليونان', code: '+30' },
{ iso: 'RO', flag: '🇷🇴', name: 'Romania', ar: 'رومانيا', code: '+40' },
{ iso: 'HU', flag: '🇭🇺', name: 'Hungary', ar: 'المجر', code: '+36' },
{ iso: 'UA', flag: '🇺🇦', name: 'Ukraine', ar: 'أوكرانيا', code: '+380' },
{ iso: 'IN', flag: '🇮🇳', name: 'India', ar: 'الهند', code: '+91' },
{ iso: 'PK', flag: '🇵🇰', name: 'Pakistan', ar: 'باكستان', code: '+92' },
{ iso: 'BD', flag: '🇧🇩', name: 'Bangladesh', ar: 'بنغلاديش', code: '+880' },
{ iso: 'CN', flag: '🇨🇳', name: 'China', ar: 'الصين', code: '+86' },
{ iso: 'JP', flag: '🇯🇵', name: 'Japan', ar: 'اليابان', code: '+81' },
{ iso: 'KR', flag: '🇰🇷', name: 'South Korea', ar: 'كوريا الجنوبية', code: '+82' },
{ iso: 'ID', flag: '🇮🇩', name: 'Indonesia', ar: 'إندونيسيا', code: '+62' },
{ iso: 'MY', flag: '🇲🇾', name: 'Malaysia', ar: 'ماليزيا', code: '+60' },
{ iso: 'SG', flag: '🇸🇬', name: 'Singapore', ar: 'سنغافورة', code: '+65' },
{ iso: 'TH', flag: '🇹🇭', name: 'Thailand', ar: 'تايلاند', code: '+66' },
{ iso: 'PH', flag: '🇵🇭', name: 'Philippines', ar: 'الفلبين', code: '+63' },
{ iso: 'VN', flag: '🇻🇳', name: 'Vietnam', ar: 'فيتنام', code: '+84' },
{ iso: 'AU', flag: '🇦🇺', name: 'Australia', ar: 'أستراليا', code: '+61' },
{ iso: 'NZ', flag: '🇳🇿', name: 'New Zealand', ar: 'نيوزيلندا', code: '+64' },
{ iso: 'CA', flag: '🇨🇦', name: 'Canada', ar: 'كندا', code: '+1' },
{ iso: 'MX', flag: '🇲🇽', name: 'Mexico', ar: 'المكسيك', code: '+52' },
{ iso: 'BR', flag: '🇧🇷', name: 'Brazil', ar: 'البرازيل', code: '+55' },
{ iso: 'AR', flag: '🇦🇷', name: 'Argentina', ar: 'الأرجنتين', code: '+54' },
{ iso: 'CL', flag: '🇨🇱', name: 'Chile', ar: 'تشيلي', code: '+56' },
{ iso: 'CO', flag: '🇨🇴', name: 'Colombia', ar: 'كولومبيا', code: '+57' },
{ iso: 'ZA', flag: '🇿🇦', name: 'South Africa', ar: 'جنوب أفريقيا', code: '+27' },
{ iso: 'NG', flag: '🇳🇬', name: 'Nigeria', ar: 'نيجيريا', code: '+234' },
{ iso: 'KE', flag: '🇰🇪', name: 'Kenya', ar: 'كينيا', code: '+254' },
{ iso: 'GH', flag: '🇬🇭', name: 'Ghana', ar: 'غانا', code: '+233' },
{ iso: 'ET', flag: '🇪🇹', name: 'Ethiopia', ar: 'إثيوبيا', code: '+251' },
{ iso: 'IR', flag: '🇮🇷', name: 'Iran', ar: 'إيران', code: '+98' },
{ iso: 'IL', flag: '🇮🇱', name: 'Israel', ar: 'إسرائيل', code: '+972' },
{ iso: 'AF', flag: '🇦🇫', name: 'Afghanistan', ar: 'أفغانستان', code: '+93' },
{ iso: 'UZ', flag: '🇺🇿', name: 'Uzbekistan', ar: 'أوزبكستان', code: '+998' },
{ iso: 'KZ', flag: '🇰🇿', name: 'Kazakhstan', ar: 'كازاخستان', code: '+7' },
];
function getFlagUrl(iso) {
return 'https://flagcdn.com/w40/' + iso.toLowerCase() + '.png';
}
function getFlagEmoji(isoOrEmoji) {
return isoOrEmoji;
}
function renderCountryOptions(list) {
const container = document.getElementById('countryOptions');
container.innerHTML = '';
list.forEach(function (c) {
const div = document.createElement('div');
div.className = 'country-option' + (c.code === document.getElementById('bookingCountryCode').value && c.name === document.getElementById('countrySelected').dataset.name ? ' selected' : '');
var displayName = (document.body.classList.contains('ar') && c.ar) ? c.ar : c.name;
div.innerHTML = '<span class="country-flag" style="font-size:20px;line-height:1;">' + c.flag + '</span><span class="country-name">' + displayName + '</span><span class="country-dial">' + c.code + '</span>';
div.onclick = function () {
document.getElementById('bookingCountryCode').value = c.code;
document.getElementById('selectedFlag').textContent = c.flag;
document.getElementById('selectedFlag').style.fontSize = '20px';
document.getElementById('selectedCode').textContent = c.code;
document.getElementById('countrySelected').dataset.name = c.name;
document.getElementById('countryDropdown').classList.remove('open');
document.getElementById('countrySearch').value = '';
renderCountryOptions(countries);
};
container.appendChild(div);
});
}
function toggleCountryDropdown() {
ensureCountryRendered();
const dd = document.getElementById('countryDropdown');
dd.classList.toggle('open');
if (dd.classList.contains('open')) {
setTimeout(function () { document.getElementById('countrySearch').focus(); }, 100);
}
}
function filterCountries(q) {
const filtered = countries.filter(function (c) {
return c.name.toLowerCase().includes(q.toLowerCase()) ||
(c.ar && c.ar.includes(q)) ||
c.code.includes(q);
});
renderCountryOptions(filtered);
}
document.addEventListener('click', function (e) {
const dd = document.getElementById('countryDropdown');
if (dd && !dd.contains(e.target)) {
dd.classList.remove('open');
}
const cdd = document.getElementById('contactCountryDropdown');
if (cdd && !cdd.contains(e.target)) {
cdd.classList.remove('open');
}
});
var _countryRendered = false;
function ensureCountryRendered() {
if (_countryRendered) return;
_countryRendered = true;
renderCountryOptions(countries);
}
var cs = document.getElementById('countrySelected');
if (cs) cs.dataset.name = 'Egypt';
var sf = document.getElementById('selectedFlag');
if (sf) {
  sf.textContent = '🇪🇬';
  sf.style.fontSize = '20px';
}
function renderContactCountryOptions(list) {
const container = document.getElementById('contactCountryOptions');
container.innerHTML = '';
list.forEach(function (c) {
const div = document.createElement('div');
div.className = 'country-option' + (c.code === document.getElementById('contactCountryCode').value && c.name === document.getElementById('contactCountrySelected').dataset.name ? ' selected' : '');
var displayName = (document.body.classList.contains('ar') && c.ar) ? c.ar : c.name;
div.innerHTML = '<span class="country-flag" style="font-size:20px;line-height:1;">' + c.flag + '</span><span class="country-name">' + displayName + '</span><span class="country-dial">' + c.code + '</span>';
div.onclick = function () {
document.getElementById('contactCountryCode').value = c.code;
document.getElementById('contactSelectedFlag').textContent = c.flag;
document.getElementById('contactSelectedFlag').style.fontSize = '20px';
document.getElementById('contactSelectedCode').textContent = c.code;
document.getElementById('contactCountrySelected').dataset.name = c.name;
document.getElementById('contactCountryDropdown').classList.remove('open');
document.getElementById('contactCountrySearch').value = '';
renderContactCountryOptions(countries);
};
container.appendChild(div);
});
}
function toggleContactCountryDropdown() {
ensureContactCountryRendered();
const dd = document.getElementById('contactCountryDropdown');
dd.classList.toggle('open');
if (dd.classList.contains('open')) {
setTimeout(function () { document.getElementById('contactCountrySearch').focus(); }, 100);
}
}
function filterContactCountries(q) {
const filtered = countries.filter(function (c) {
return c.name.toLowerCase().includes(q.toLowerCase()) ||
(c.ar && c.ar.includes(q)) ||
c.code.includes(q);
});
renderContactCountryOptions(filtered);
}
var _contactCountryRendered = false;
function ensureContactCountryRendered() {
if (_contactCountryRendered) return;
_contactCountryRendered = true;
renderContactCountryOptions(countries);
}
var ccs = document.getElementById('contactCountrySelected');
if (ccs) ccs.dataset.name = 'Egypt';
var csf = document.getElementById('contactSelectedFlag');
if (csf) {
  csf.textContent = '🇪🇬';
  csf.style.fontSize = '20px';
}
var revealTarget = document.querySelectorAll(".reveal-up");
var revealObs = new IntersectionObserver(function (entries, observer) {
entries.forEach(function (entry) {
if (entry.isIntersecting) {
entry.target.classList.add("active");
observer.unobserve(entry.target);
}
});
}, { rootMargin: "0px" });
revealTarget.forEach(function (el) {
revealObs.observe(el);
});
var dot = document.createElement("div");
dot.className = "cursor-dot";
document.body.appendChild(dot);
var outline = document.createElement("div");
outline.className = "cursor-outline";
document.body.appendChild(outline);
var cursorX = 0, cursorY = 0, cursorRaf = false;
document.addEventListener("mousemove", function (e) {
cursorX = e.clientX;
cursorY = e.clientY;
if (!cursorRaf) {
cursorRaf = true;
requestAnimationFrame(function () {
var tx = cursorX + "px";
var ty = cursorY + "px";
dot.style.transform = "translate(calc(" + tx + " - 50%), calc(" + ty + " - 50%))";
outline.style.transform = "translate(calc(" + tx + " - 50%), calc(" + ty + " - 50%))";
cursorRaf = false;
});
}
});
document.querySelectorAll("a, button, .project-card, .service-card, .contact-item, .lang-btn").forEach(function (el) {
el.addEventListener("mouseenter", function () {
outline.classList.add("hover");
dot.classList.add("hover");
});
el.addEventListener("mouseleave", function () {
outline.classList.remove("hover");
dot.classList.remove("hover");
});
});
var revealElements = document.querySelectorAll('.section-title, .service-card, .about-inner > div, .contact-inner > div, .form-wrap > div, .portfolio-grid');
revealElements.forEach(function (el) { el.classList.add('reveal-up'); });
var revealObserver = new IntersectionObserver(function (entries, observer) {
entries.forEach(function (entry) {
if (entry.isIntersecting) {
entry.target.classList.add("active");
observer.unobserve(entry.target);
}
});
}, { rootMargin: "0px" });
revealElements.forEach(function (el) { revealObserver.observe(el); });
document.querySelectorAll('.service-card').forEach(function (card) {
var glow = document.createElement("div");
glow.className = "card-glow-effect";
card.insertBefore(glow, card.firstChild);
let cardRect;
card.addEventListener("mouseenter", function () {
cardRect = card.getBoundingClientRect();
});
card.addEventListener("mousemove", function (e) {
if (!ticking && cardRect) {
requestAnimationFrame(() => {
var x = e.clientX - cardRect.left - 150;
var y = e.clientY - cardRect.top - 150;
glow.style.transform = "translate(" + x + "px, " + y + "px)";
ticking = false;
});
ticking = true;
}
});
});
(function () {
var allCards = document.querySelectorAll('#portfolioGrid .project-card');
function filterPortfolio(category) {
var visibleIdx = 0;
allCards.forEach(function (card) {
var match = category === 'all' || card.dataset.category === category;
if (match) {
card.classList.remove('pf-hidden');
card.style.opacity = '0';
card.style.transform = 'translateY(30px) scale(0.95)';
card.style.pointerEvents = 'none';
(function (c, idx) {
setTimeout(function () {
c.style.transition = 'opacity 0.7s ease, transform 0.7s cubic-bezier(0.22,1,0.36,1)';
c.style.opacity = '1';
c.style.transform = 'translateY(0) scale(1)';
setTimeout(function () { c.style.pointerEvents = 'auto'; }, 700);
}, idx * 100);
})(card, visibleIdx);
visibleIdx++;
} else {
card.classList.add('pf-hidden');
card.style.opacity = '0';
card.style.transform = 'translateY(20px) scale(0.95)';
card.style.transition = 'opacity 0.3s ease, transform 0.3s ease';
}
});
if (window._rebuildVidCards) window._rebuildVidCards();
}
allCards.forEach(function (card) {
var thumb = card.querySelector('.card-thumb');
let ticking = false;
let cardRect;
card.addEventListener('mouseenter', function () {
cardRect = card.getBoundingClientRect();
});
card.addEventListener('mousemove', function (e) {
if (!ticking && cardRect) {
requestAnimationFrame(() => {
var x = e.clientX - cardRect.left;
var y = e.clientY - cardRect.top;
var centerX = cardRect.width / 2;
var centerY = cardRect.height / 2;
var deltaX = (x - centerX) / centerX;
var deltaY = (y - centerY) / centerY;
card.style.transform = 'perspective(1000px) rotateY(' + (deltaX * 6) + 'deg) rotateX(' + (deltaY * -6) + 'deg) translateY(-8px) scale(1.02)';
if (thumb) {
thumb.style.transform = 'scale(1.1) translate(' + (deltaX * -10) + 'px, ' + (deltaY * -10) + 'px)';
}
ticking = false;
});
ticking = true;
}
});
card.addEventListener('mouseleave', function () {
card.style.transform = 'perspective(1000px) rotateY(0deg) rotateX(0deg) translateY(0) scale(1)';
if (thumb) {
thumb.style.transform = 'scale(1) translate(0, 0)';
}
});
});
filterPortfolio('all');
})();
(function () {
var wistiaApiHandles = {};
var projectData = [
{ id: '01', wistiaId: 'hv8s1hoqzx', aspect: 'vertical', thumb: 'assets/images/تبارك.png', title_en: 'Al Salmiya Hotel', title_ar: 'فندق السالمية', company_en: 'Tabarak United', company_ar: 'تبارك المتحدة', category: 'hotel' },
{ id: '02', wistiaId: 'he0nwj9mie', aspect: 'vertical', thumb: 'assets/images/اراضي تجارية- الصجعة الشارقة.png', title_en: 'Al Sajaa Commercial Land', title_ar: 'أراضي تجارية الصجعة الشارقة', company_en: 'Ewan Realestate', company_ar: 'إيوان العقارية', category: 'commercial' },
{ id: '03', wistiaId: 'nfpm9hy62j', aspect: 'vertical', thumb: 'assets/images/شمسي.png', title_en: 'Karbala Investment Land', title_ar: 'أرض استثمارية كربلاء العراق', company_en: 'SHAMSI', company_ar: 'شمسي', category: 'investment' },
{ id: '04', wistiaId: 'gyoo4pn2tv', aspect: 'landscape', thumb: 'assets/images/مشاريع مدينة 15 مايو.png', title_en: '15 Mayo City Projects', title_ar: 'مشاريع مدينة 15 مايو', company_en: 'Al Salam Real Estate Investment ', company_ar: 'السلام للاستثمار العقاري ', category: 'development' },
{ id: '05', wistiaId: '7vq8s3nyb5', aspect: 'landscape', thumb: 'https://fast.wistia.com/embed/medias/7vq8s3nyb5/swatch', title_en: 'Golden Club', title_ar: 'جولدن كلوب', company_en: 'Al Salam Real Estate Investment', company_ar: 'شركة السلام للاستثمار العقاري', category: 'other' },
{ id: '06', wistiaId: 'amu5e7gs6l', aspect: 'landscape', thumb: 'https://fast.wistia.com/embed/medias/amu5e7gs6l/swatch', title_en: 'Mayo Mall', title_ar: 'مايو مول', company_en: 'Al Salam Real Estate Investment', company_ar: 'شركة السلام للاستثمار العقاري', category: 'other' },
{ id: '07', wistiaId: 'ojcqr4ycsd', aspect: 'landscape', thumb: 'https://fast.wistia.com/embed/medias/ojcqr4ycsd/swatch', title_en: 'Medina Mall', title_ar: 'المدينة مول', company_en: 'Al Salam Real Estate Investment', company_ar: 'شركة السلام للاستثمار العقاري', category: 'other' },
{ id: '08', wistiaId: '', aspect: 'vertical', thumb: 'assets/images/placeholder.png', title_en: 'New Project 8', title_ar: 'مشروع جديد 8', company_en: 'Developer Name', company_ar: 'اسم المطور', category: 'other' },
{ id: '09', wistiaId: '', aspect: 'vertical', thumb: 'assets/images/placeholder.png', title_en: 'New Project 9', title_ar: 'مشروع جديد 9', company_en: 'Developer Name', company_ar: 'اسم المطور', category: 'other' },
{ id: '10', wistiaId: '', aspect: 'vertical', thumb: 'assets/images/placeholder.png', title_en: 'New Project 10', title_ar: 'مشروع جديد 10', company_en: 'Developer Name', company_ar: 'اسم المطور', category: 'other' }
];
var modal = document.getElementById('reels-modal');
var container = document.getElementById('reels-container');
var closeBtn = document.getElementById('reels-close');
var trigger = document.getElementById('open-reels-trigger');
var builtOnce = false;
var reelsObserver = null;
function createSlide(item) {
var slide = document.createElement('div');
slide.className = 'reel-slide';
if (item.aspect && item.aspect.toLowerCase() === 'landscape') slide.classList.add('is-landscape');
slide.dataset.id = item.id;
var videoWrap = document.createElement('div');
videoWrap.className = 'reel-video-wrap';
videoWrap.innerHTML = `
<div class="brand-protection" style="position:absolute; inset:0; z-index:10; background:transparent;"></div>
<div class="wistia_embed wistia_async_${item.wistiaId} videoFoam=true qualityMin=1080 qualityMax=1080 playbar=false smallPlayButton=false volumeControl=false fullscreenButton=false controlsVisibleOnLoad=false settingsControl=false contextMenu=false branding=false" style="height:100%;width:100%; position:relative;">
&nbsp;
</div>
`;
window._wq = window._wq || [];
_wq.push({
id: item.wistiaId, onReady: function (video) {
wistiaApiHandles[item.id] = video;
video.muted(true);
video.loop(true);
try {
if (video.contextMenu) video.contextMenu(false);
if (video.playbar) video.playbar.hide();
if (typeof video.quality === 'function') video.quality(1080);
} catch (e) { }
}
});
var info = document.createElement('div');
info.className = 'reel-info';
var textWrap = document.createElement('div');
textWrap.className = 'reel-text-wrap';
var title = document.createElement('div');
title.className = 'reel-title';
title.setAttribute('data-en', item.title_en);
title.setAttribute('data-ar', item.title_ar);
title.textContent = document.documentElement.lang === 'ar' ? item.title_ar : item.title_en;
var comp = document.createElement('div');
comp.className = 'reel-company';
comp.setAttribute('data-en', item.company_en);
comp.setAttribute('data-ar', item.company_ar);
comp.textContent = document.documentElement.lang === 'ar' ? item.company_ar : item.company_en;
textWrap.appendChild(title);
textWrap.appendChild(comp);
var bta = document.createElement('button');
bta.className = 'btn-booknow';
bta.style.pointerEvents = 'auto';
bta.innerHTML = `
<svg class="icon-booking" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
<path d="M19 4H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2z"/>
<path d="M16 2v4"/><path d="M8 2v4"/><path d="M3 10h18"/><path d="M9.5 15l2 2 4-4"/>
</svg>
<span data-en="BOOK NOW" data-ar="احجز الآن">${document.documentElement.lang === 'ar' ? 'احجز الآن' : 'BOOK NOW'}</span>
<svg class="icon-arrow" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
<line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/>
</svg>
`;
bta.onclick = function (e) {
e.stopPropagation();
closeReels();
setTimeout(function () {
document.getElementById('pricing').scrollIntoView({ behavior: 'smooth' });
}, 500);
};
info.appendChild(textWrap);
info.appendChild(bta);
slide.appendChild(videoWrap);
slide.appendChild(info);
return slide;
}
var wistiaLoaded = false;
function ensureWistia() {
if (wistiaLoaded) return;
wistiaLoaded = true;
var s = document.createElement('script');
s.src = "https://fast.wistia.net/assets/external/E-v1.js";
s.async = true;
document.head.appendChild(s);
}
function buildReels() {
if (builtOnce) return;
builtOnce = true;
projectData.filter(function (item) {
return item.wistiaId && item.wistiaId.trim() !== "";
}).forEach(function (item) {
container.appendChild(createSlide(item));
});
var options = { root: container, threshold: 0.6 };
reelsObserver = new IntersectionObserver(function (entries) {
entries.forEach(function (entry) {
var slide = entry.target;
var handle = wistiaApiHandles[slide.dataset.id];
if (entry.isIntersecting) {
slide.classList.add('active');
if (handle) handle.play();
} else {
slide.classList.remove('active');
if (handle) handle.pause();
}
});
}, options);
container.querySelectorAll('.reel-slide').forEach(function (slide) {
reelsObserver.observe(slide);
});
}
trigger.addEventListener('pointerenter', ensureWistia, { once: true });
trigger.addEventListener('mouseover', ensureWistia, { once: true });
function openReels() {
ensureWistia();
buildReels();
modal.style.display = 'flex';
document.body.style.overflow = 'hidden';
requestAnimationFrame(function () {
requestAnimationFrame(function () {
modal.classList.add('open');
showTutorial();
});
});
}
function closeReels() {
modal.classList.remove('open');
for (var id in wistiaApiHandles) {
if (wistiaApiHandles[id]) wistiaApiHandles[id].pause();
}
setTimeout(function () {
modal.style.display = 'none';
document.body.style.overflow = '';
}, 400);
}
trigger.addEventListener('click', openReels);
closeBtn.addEventListener('click', closeReels);
var tutorial = document.getElementById('reels-tutorial');
var tutorialTimer;
function showTutorial() {
tutorial.classList.add('show');
clearTimeout(tutorialTimer);
tutorialTimer = setTimeout(hideTutorial, 3500);
}
function hideTutorial() {
tutorial.classList.remove('show');
clearTimeout(tutorialTimer);
}
container.addEventListener('scroll', function () {
if (tutorial.classList.contains('show')) hideTutorial();
}, { passive: true });
modal.addEventListener('click', function () {
if (tutorial.classList.contains('show')) hideTutorial();
}, { passive: true });
var _cachedSlideWidth = 0;
function getSlideWidth() {
if (!_cachedSlideWidth) {
var slide = container.querySelector('.reel-slide');
_cachedSlideWidth = slide ? slide.offsetWidth + 40 : 400;
}
return _cachedSlideWidth;
}
function scrollReels(direction) {
var slideWidth = getSlideWidth();
container.scrollBy({ left: direction === 'next' ? slideWidth : -slideWidth, behavior: 'smooth' });
hideTutorial();
}
document.querySelector('.reels-nav-hint--left').addEventListener('click', function () { scrollReels('prev'); });
document.querySelector('.reels-nav-hint--right').addEventListener('click', function () { scrollReels('next'); });
document.querySelectorAll('.reels-nav-hint').forEach(function (h) {
h.style.pointerEvents = 'auto';
h.style.cursor = 'pointer';
});
var isDown = false;
var startX, scrollLeft, rafId;
container.addEventListener('mousedown', function (e) {
isDown = true;
container.style.cursor = 'grabbing';
startX = e.pageX - container.offsetLeft;
scrollLeft = container.scrollLeft;
});
container.addEventListener('mouseleave', function () { isDown = false; container.style.cursor = 'grab'; });
container.addEventListener('mouseup', function () { isDown = false; container.style.cursor = 'grab'; });
container.addEventListener('mousemove', function (e) {
if (!isDown) return;
e.preventDefault();
var x = e.pageX - container.offsetLeft;
var walk = (x - startX) * 2;
cancelAnimationFrame(rafId);
rafId = requestAnimationFrame(function () {
container.scrollLeft = scrollLeft - walk;
});
});
container.style.cursor = 'grab';
window.filterPortfolio = function (filter) {
};
var observerLang = new MutationObserver(function () {
var isAr = document.documentElement.lang === 'ar';
container.querySelectorAll('.reel-title').forEach(function (el) {
el.textContent = isAr ? el.getAttribute('data-ar') : el.getAttribute('data-en');
});
container.querySelectorAll('.reel-company').forEach(function (el) {
el.textContent = isAr ? el.getAttribute('data-ar') : el.getAttribute('data-en');
});
container.querySelectorAll('.btn-booknow span').forEach(function (el) {
el.textContent = isAr ? el.getAttribute('data-ar') : el.getAttribute('data-en');
});
var launchCard = document.querySelector('.reels-launch-card');
if (launchCard) {
launchCard.querySelectorAll('[data-en]').forEach(function (el) {
el.textContent = isAr ? el.getAttribute('data-ar') : el.getAttribute('data-en');
});
}
});
observerLang.observe(document.documentElement, { attributes: true, attributeFilter: ['lang'] });
})();
(function () {
const animateCounter = (counter) => {
if (counter.dataset.animated) return;
counter.dataset.animated = "true";
const target = +counter.getAttribute('data-target');
const speed = 100;
const increment = target / speed;
let currentCount = 0;
const originalText = counter.textContent || '';
const hasK = originalText.includes('K');
const hasPlus = originalText.includes('+');
let suffix = hasK ? 'K+' : (hasPlus ? '+' : '');
if (counter.classList.contains('counter')) suffix = '';
const update = () => {
currentCount += increment;
if (currentCount < target) {
counter.textContent = Math.ceil(currentCount) + suffix;
setTimeout(update, 20);
} else {
counter.textContent = target + suffix;
}
};
update();
};
const statsObserver = new IntersectionObserver((entries, observer) => {
entries.forEach(entry => {
if (entry.isIntersecting) {
const nums = entry.target.querySelectorAll('.stat-num, .counter');
if (nums.length > 0) {
nums.forEach(num => animateCounter(num));
} else if (entry.target.classList.contains('stat-num') || entry.target.classList.contains('counter')) {
animateCounter(entry.target);
}
observer.unobserve(entry.target);
}
});
}, { threshold: 0.2 });
document.querySelectorAll('.stat-card, .ph-stat, .stats-row, .stat-num, .counter').forEach(el => {
statsObserver.observe(el);
});
})();
(function () {
const viewerTexts = {
en: [" others viewing", " people booking now", " watching currently"],
ar: [" أشخاص يشاهدون الآن", " عملاء يحجزون الآن", " يشاهدون حالياً"]
};
const textEl = document.querySelector('.v-text');
if (!textEl) return;
function updateViewers() {
const isAr = document.documentElement.lang === 'ar';
const count = Math.floor(Math.random() * 5) + 3;
const msg = isAr ? viewerTexts.ar[Math.floor(Math.random() * 3)] : viewerTexts.en[Math.floor(Math.random() * 3)];
textEl.style.opacity = 0;
setTimeout(() => {
textEl.textContent = count + msg;
textEl.style.opacity = 0.8;
}, 500);
}
setInterval(updateViewers, 7000);
updateViewers();
})();
(function () {
const burger = document.getElementById('menu-toggle');
const navLinks = document.querySelector('.nav-links');
const links = document.querySelectorAll('.nav-links a');
if (!burger || !navLinks) return;
function toggleMenu() {
burger.classList.toggle('active');
navLinks.classList.toggle('active');
document.body.style.overflow = navLinks.classList.contains('active') ? 'hidden' : '';
}
  if (burger) {
    burger.addEventListener('click', toggleMenu);
  }
  if (links) {
    links.forEach(link => {
      link.addEventListener('click', () => {
        if (navLinks && navLinks.classList.contains('active')) {
          toggleMenu();
        }
      });
    });
  }
})();
